{
  "name": "india-geo-saas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "npm run dev --workspace=apps/backend",
    "frontend": "npm run dev --workspace=apps/frontend",
    "vercel-build": "cd apps/backend && npx prisma generate"
  }
}